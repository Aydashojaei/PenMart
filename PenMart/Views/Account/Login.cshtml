@model PenMart.Models.LoginViewModel
@{
    ViewData["Title"] = "ورود به سایت";
}

<div class="login-container">
    <div class="login-card shadow">
        <h2 class="text-center mb-4">ورود به پن‌مارت 🛍️</h2>

        <form asp-action="Login">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="form-group mb-3">
                <label asp-for="Email" class="form-label">ایمیل</label>
                <input asp-for="Email" class="form-control" placeholder="example@gmail.com" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Password" class="form-label">رمز عبور</label>
                <input asp-for="Password" class="form-control" type="password" placeholder="********" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <div class="form-check mb-3">
                <input class="form-check-input" asp-for="RemmemberMe" />
                <label class="form-check-label" asp-for="RemmemberMe">مرا به خاطر بسپار</label>
            </div>

            <button type="submit" class="btn btn-primary w-100">ورود</button>

            <p class="text-center mt-3">
                حساب کاربری ندارید؟ <a asp-action="Register" asp-controller="Account" class="text-primary">ثبت‌نام کنید</a>
            </p>
        </form>
    </div>
</div>

<style>
    body {
        background: linear-gradient(135deg, #eaf6fb, #f8fbfd);
        font-family: 'Sahel', sans-serif;
    }

    .login-container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 90vh;
    }

    .login-card {
        background: white;
        border-radius: 20px;
        padding: 40px;
        width: 100%;
        max-width: 400px;
    }

    .form-control {
        border-radius: 10px;
        padding: 10px;
    }

    .btn-primary {
        background-color: #5bbad5;
        border: none;
        padding: 10px;
        border-radius: 10px;
        transition: 0.3s;
    }

        .btn-primary:hover {
            background-color: #3aa6c5;
        }

    a.text-primary:hover {
        text-decoration: underline;
    }
</style>
